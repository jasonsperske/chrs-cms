export type BookVariation = {
    title: string,
    author: string,
    publishedBy: string,
    yearPublished: number,
    monthPublished: string,
    mediaType: string,
    serialNumber: string,
    catalogNumber: string,
    confidence: number
}